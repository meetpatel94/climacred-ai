# Optional: absolute backend URL. Leave empty/undefined to use the same origin
# (the vite dev/preview server proxies /api to http://localhost:8000).
VITE_API_URL=

# Gemini key is configured ONLY in backend/.env - never in the frontend or git.

